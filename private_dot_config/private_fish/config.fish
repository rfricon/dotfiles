# # Set up Node Version Manager
# set -x NVM_DIR "$HOME/.nvm"                            # You can change this if you want.
# set -x NVM_SOURCE "/usr/share/nvm"                     # The AUR package installs it to here.
# [ -s "$NVM_SOURCE/nvm.sh" ] && bass . "$NVM_SOURCE/nvm.sh"  # Load NVM

# bass source /usr/share/nvm/init-nvm.sh

test -s /home/romain/.nvm/nvm.fish; and source /home/romain/.nvm/nvm.fish
